import { ModuleNameFormatterPipe } from './module-name-formatter.pipe';

describe('ModuleNameFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new ModuleNameFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
